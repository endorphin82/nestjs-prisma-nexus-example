# Migration `20200627162604-init`

This migration has been generated by Eugene Barannik at 6/27/2020, 4:26:04 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "UserRole" AS ENUM ('ADMIN', 'EMPLOYEE');

CREATE TYPE "Currency" AS ENUM ('USD', 'EUR', 'RUB');

CREATE TABLE "public"."User" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"email" text   ,"id" integer  NOT NULL ,"name" text   ,"password" text   ,"roles" "UserRole"[]  ,"updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Account" (
"balance" Decimal(65,30)  NOT NULL DEFAULT 0,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"currency" "Currency" NOT NULL DEFAULT E'USD',"id" integer  NOT NULL ,"name" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,"userId" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Transaction" (
"accountId" integer  NOT NULL ,"amount" Decimal(65,30)  NOT NULL ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" integer  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

ALTER TABLE "public"."Account" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Transaction" ADD FOREIGN KEY ("accountId")REFERENCES "public"."Account"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200627162604-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,51 @@
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id        Int         @id
+  createdAt DateTime    @default(now())
+  updatedAt DateTime    @updatedAt
+  name      String?
+  accounts  Account[]
+  email     String?     @unique
+  password  String?
+  roles     UserRole[]
+}
+
+model Account {
+  id            Int           @id
+  createdAt     DateTime      @default(now())
+  updatedAt     DateTime      @updatedAt
+  name          String
+  currency      Currency      @default(USD)
+  balance       Float         @default(0)
+  user          User          @relation(fields:[userId])
+  userId        Int           
+  transactions  Transaction[]
+}
+
+model Transaction {
+  id        Int      @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  amount    Float
+  account   Account  @relation(fields: [accountId])
+  accountId Int
+}
+
+enum UserRole {
+  ADMIN
+  EMPLOYEE
+}
+
+enum Currency {
+  USD
+  EUR
+  RUB
+}
```


