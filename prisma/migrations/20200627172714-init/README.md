# Migration `20200627172714-init`

This migration has been generated by Eugene Barannik at 6/27/2020, 5:27:14 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "UserRole" AS ENUM ('ADMIN', 'EMPLOYEE');

CREATE TYPE "Currency" AS ENUM ('USD', 'EUR', 'RUB');

CREATE TABLE "test2"."User" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"email" text   ,"id" text  NOT NULL ,"name" text   ,"password" text   ,"roles" "UserRole"[]  ,"updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "test2"."Account" (
"balance" Decimal(65,30)  NOT NULL DEFAULT 0,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"currency" "Currency" NOT NULL DEFAULT E'USD',"id" text  NOT NULL ,"name" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,"userId" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "test2"."Transaction" (
"accountId" text  NOT NULL ,"amount" Decimal(65,30)  NOT NULL ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "test2"."User"("email")

ALTER TABLE "test2"."Account" ADD FOREIGN KEY ("userId")REFERENCES "test2"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "test2"."Transaction" ADD FOREIGN KEY ("accountId")REFERENCES "test2"."Account"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200627172714-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,51 @@
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id        String              @id @default(cuid())
+  createdAt DateTime    @default(now())
+  updatedAt DateTime    @updatedAt
+  name      String?
+  accounts  Account[]
+  email     String?     @unique
+  password  String?
+  roles     UserRole[]
+}
+
+model Account {
+  id            String              @id @default(cuid())
+  createdAt     DateTime      @default(now())
+  updatedAt     DateTime      @updatedAt
+  name          String
+  currency      Currency      @default(USD)
+  balance       Float         @default(0)
+  user          User          @relation(fields:[userId])
+  userId        String
+  transactions  Transaction[]
+}
+
+model Transaction {
+  id        String              @id @default(cuid())
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  amount    Float
+  account   Account  @relation(fields: [accountId])
+  accountId String
+}
+
+enum UserRole {
+  ADMIN
+  EMPLOYEE
+}
+
+enum Currency {
+  USD
+  EUR
+  RUB
+}
```


